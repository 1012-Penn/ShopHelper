# ch06 开发过程留痕(分流器正式版:指代消解 / 意图识别 / 退款售后确定性子流程)

## 2026-09-08 · brainstorm 定稿

- **用户关键原话**:需求全文见会话(指代消解/Query 改写扩写/意图识别四件套/退款售后确定性子流程/槽位处理/订单选择器+退款单前端;「意图识别用 LLM prompt 路线,不训分类模型」「缺订单号不让模型猜」「扩写放检索侧现查现用,库里知识只留一份」);澄清问题(订单数据源/停走机制/退款单落点)用户未逐项作答,回复**「继续」**——按 ch05「后续你都自己操作,我不过目了」先例,三个决策点按推荐项自决,全程留痕可纠偏。
- **关键产出**:
  - 分类为 Architectural 路径;现状摸底:占位分流器在 `app/graph/entry.py`(resolve 透传/intent 无置信度/退款退货走 retrieve),订单是 `query_order` 随机 mock 无订单表,前端单页 SSE 已有 actions 按钮模式;
  - **spike 实测排除 `interrupt()`**:`/tmp/spike_interrupt.py` 在 venv(py3.10 + langgraph 1.2.11)下复现 `interrupt()` → `langgraph/config.py::get_config` 抛 `RuntimeError("Called get_config outside of a runnable context")`——与 ch05 `get_stream_writer` 被禁是同一函数同一 py3.10 async 守卫;停走机制定稿**无状态回传**(选择器数据+补全问题随帧下发,点选随请求带回,零服务端跨回合状态);
  - Context7 核对条件边/TypedDict State 语义,与现有 route 写法一致,新增 API 面为零;
  - spec 落盘:`docs/superpowers/specs/2026-09-08-ch06-router-production-design.md`(自审过:占位符/一致性/歧义/范围;自审补强一处——验收 3 的指代补全需前置上下文,真机验收脚本要先铺垫一轮商品问答)。
- **三个自决决策点**:①订单数据源=固定 mock 目录(`app/orders.py` 单一来源,`GET /api/orders`+稳定化 query_order+子流程三处共用,不建表);②停走=无状态回传(interrupt 被 spike 排除/服务端 pending slot 与 initial_state 防残留机制冲突);③退款单=复用 `POST /api/tickets`(type=售后,描述=订单+原因类目+备注)。
- **拒绝或纠偏**:无(用户未逐项表态,推荐项即定稿)。
- **翻车与返工**:无返工;一次前置排雷——若不 spike 直选 interrupt(),实现期必撞 ch05 同款 py3.10 雷,属计划期消 retour。

## 2026-09-08 · 计划评审通过 + 执行开始

- **用户关键原话**:「继续」(承接 brainstorm 段的全自主授权;spec/计划评审门由我自审代行)。
- **关键产出**:`docs/superpowers/plans/2026-09-08-ch06-router-production.md`(10 任务;1 订单目录/query_order 稳定化/orders 端点,2 prompt 三件套+宽松解析,3 entry 节点升级,4 子流程五节点,5 agent 窄化+refund_form+编号防撞,6 组图+resume 契约,7 前端 Vibe,8 评估集+真跑报告,9 code review,10 真机验收+finish)。自审结论:spec 各节均有对应任务、类型/签名跨任务一致;自审修正两处——route 末行三元串写法换清晰版、Task 1 测试草稿占位行给正式版。
- **拒绝或纠偏**:writing-plans 默认推荐 subagent-driven;沿用 ch05 结论选 inline(executing-plans)——本计划与 ch04/ch05 替身模式(GraphChatModel/EchoResolver/StubExpand)强耦合、共享上下文价值高,记为有意偏离,非用户否决。
- **翻车与返工**:无(计划期)。

## 2026-09-08 · Task 1 完成(订单目录单一来源)

- **用户关键原话**:「缺订单号不让模型猜,执行阶段弹订单选择器让用户点选回填」(订单数据源自决:固定 mock 目录,延续 ch02 不建表哲学)。
- **关键产出**:`app/orders.py`(ORDER_CATALOG 三单/get_order 未命中返 error dict/list_orders/extract_order_id 上下文优先+裸4-6位兜底)、`app/routers/orders.py`(GET /api/orders)、`app/tools/definitions.py` query_order 随机 mock 退役改调 get_order、`app/config.py` 增意图三设置(escalation 默认关)、`app/llm.py` make_extract_model 增 model 覆盖参数。`tests/test_ch06_orders.py` 6 passed;全量 **201 passed, 1 xfailed**(旧套件零破坏)。
- **拒绝或纠偏**:无。
- **翻车与返工**:无。

## 2026-09-08 · Task 2 完成(prompt 三件套+意图宽松解析)

- **用户关键原话**:「意图识别用 LLM prompt 路由……按四件套写:枚举成选择题/强制 JSON 两个字段/边界 few-shot/留其他兜底」;「意图输出的 JSON 稳定可解析」。
- **关键产出**:`app/prompts.py`(INTENT_PROMPT 四件套重写:八选一枚举+判类口径+4 组边界样例+其他口径;RESOLVE_PROMPT/EXPAND_PROMPT 定稿,文案无花括号;ResolvedQuestion/ExpandedQuestions→ExpandedQueries 结构化模型)、`app/graph/entry.py` 增 parse_intent(剥代码围栏→json.loads→枚举校验→confidence 截断 [0,1],畸形/超纲→(其他,0.0,True))。`tests/test_ch06_prompts.py` 7 passed。旧 INTENT_FALLBACK(商品咨询)同步换「其他」,test_ch05_entry 迁移归 Task 3。
- **拒绝或纠偏**:无。
- **翻车与返工**:无。

## 2026-09-08 · Task 3 完成(entry 节点升级)

- **用户关键原话**:「用 LLM 结合对话历史,把『它能退吗』这种带指代的话补全成……完整问题;已完整的原样透传」「置信度低才升级大模型重判一次」「拿不准就归它(其他),别硬塞」。
- **关键产出**:`app/graph/entry.py` 整体重写——make_resolve_node(resolver)(resume 旁路不走 LLM/LLM 异常兜底透传/changed 才发 resolved 帧)、make_intent_node(model, escalator, floor)(小→大降级路,escalated/malformed 进 trace)、route 八出口(退款退货/售后→prepare_order,其他→agent);`tests/helpers.py` 增 EchoResolver/StubExpand/graph_state 共享底座;builder 最小接线(生产 resolver 由 make_extract_model 现造,Task 6 重排)。`tests/test_ch06_router.py` 10 passed;旧 test_ch05_entry 六用例被 ch06 文件全覆盖,git rm。
- **拒绝或纠偏**:无。
- **翻车与返工**:一次自伤——helpers.py 两次锚点编辑把 StubExpand 弄出空类壳+docstring 与方法挤同行,IndentationError/SyntaxError 连爆;读文件定位后删壳+补换行修复。教训:同文件连续 Edit 的锚点必须唯一且含后文。

## 2026-09-08 · Task 4 完成(退款售后子流程五节点)

- **用户关键原话**:「先拿订单数据、再做 Query 扩写并强制检索政策条款,最后只把『这一单能不能退』交给主力 Agent 判断」「几条一起检索再去重合并」。
- **关键产出**:`app/graph/refund.py`(make_refund_nodes 五节点:prepare_order 纯 regex 槽位检查 resume 优先/ask_order 发 order_selector 帧且只落 user 消息/fetch_order 确定性取数/expand 结构化扩写失败兜底原句/policy_retrieve 逐条 hybrid_rerank(use_rewrite=False)→chunk_id 去重留最高分→截 top_k 重编号+citations 帧,证据全空落池仍进 Agent);ChatState 七个新字段。`tests/test_ch06_refund.py` 9 passed;全量 **221 passed, 1 xfailed**。
- **拒绝或纠偏**:无。
- **翻车与返工**:一次测试数据算错——去重合并断言写成 [11,13,12],实际合并分 14(0.6)>12(0.5) 应为 [11,13,14],实现无误、改断言(sys systematic-debugging:先看失败值再定谁错)。

## 2026-09-08 · Task 5 完成(agent 窄化+refund_form+编号防撞)

- **用户关键原话**:「最后只把『这一单能不能退』交给主力 Agent 判断」「退款原因不追问,提交退款单时从固定类目里自选」。
- **关键产出**:agent.py(NARROW_INSTRUCTIONS 按意图窄化+订单数据 JSON 进 System;收敛时非拒答且退款子流程→refund_form action;**citations 继承既有证据**——前端 citations 帧是覆盖语义,agent 轮内 query_faq 重发必须带全量,顺手修掉 ch05 知识路径的隐性撞号;n_offset=len(evidence) 续排)、simple.py ACTION_LABELS+申请退款、logging_node actions item 带 order_id。tests/test_ch06_agent.py 6 passed;旧 test_citations_renumbered 按 ch06 新契约迁移(工具 JSON 自证据数后续排);全量 **227 passed, 1 xfailed**。
- **拒绝或纠偏**:无。
- **翻车与返工**:①测试底座误用——graph_state 共享默认 refund_flow=False,子流程用例需本地包装注入订单默认值;②ch04 旧断言撞新契约(n_offset 续排后工具 JSON 编号从 3 起),确认是新契约(消撞号)后迁移断言;③发现 e2e 测试里生产 resolver 走真网 401(兜底透传不炸),Task 6 conftest 注入 EchoResolver 后消除。

## 2026-09-08 · Task 6 完成(组图+resume 契约+端到端)

- **用户关键原话**:「点选后自动回填、流程接着走」「浏览器里不带订单号问退款,聊天页弹出订单选择器」。
- **关键产出**:builder.py 全量重排(resume 旁路条件边 resolve→prepare_order;prepare_order 槽位条件边;fetch→expand→policy_retrieve→agent 链;ask_order→log;resolver/expander 生产自建/测试注入)、schemas.OrderResume(pattern 校验 3-6 位)、chat.py resume 注入、conftest 注入 EchoResolver/StubExpand(测试彻底不出网)。tests/test_ch06_chat_api.py 5 passed;全量 **232 passed, 1 xfailed**。
- **拒绝或纠偏**:无。
- **翻车与返工**:两处——①测试数据写反:补全问法里带了「订单1001」导致槽位已满不出选择器,改成「我买的无线耳机能退吗」;②**设计缺口(实质返工)**:resume 旁路跳过 intent 节点,但 agent 窄化/refund_form 都判 state["intent"],resume 轮恒空——意图属挂起上下文,必须随无状态回传往返:order_selector 帧增 intent 字段、OrderResume 增 intent(Literal 退款退货/售后)、initial_state 注入。契约演进记入 spec 实现期偏差(finish 阶段回填)。

## 2026-09-08 · Task 7 完成(前端:订单选择器/resolved 灰字/退款表单,Vibe 例外不走流程)

- **用户关键原话**:「订单选择器做成聊天流里可点选的订单卡片,点选后自动回填、流程接着走;退款单是一个简单表单,退款原因从固定类目里下拉选」。
- **关键产出**:static/index.html——send() 的 fetch/SSE 循环抽成 streamChat(payload, bubble) 供点选复用(顺带修「(空回复)」误显示:选择器轮有 order-bar 时不提示);resolved 帧→灰字「已理解:…」;order_selector 帧→三张订单卡(单号/商品/金额/状态),点选置灰锁定+自动 POST resume(order_id/question/intent);refund_form action→内嵌表单(原因六类下拉+备注),提交 POST /api/tickets(售后)回填工单号。node --check JS 语法过;效果终验随 Task 10 浏览器自动化。
- **拒绝或纠偏**:无(Vibe 模式,效果描述权在用户,后续随验随改)。
- **翻车与返工**:pickOrder 首版引用了不存在的 order._el 做选中高亮,改 card 参数直传;一次编辑中断未落盘,重查文件状态后重放。
