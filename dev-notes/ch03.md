# ch03 开发留痕:RAG 基础——知识库 + 向量检索(query_faq 内核升级)

> 工作约定:每完成一个阶段追记一段,每段记四样:用户关键原话、关键产出、拒绝或纠偏、翻车与返工。

---

## 2026-09-05 · Brainstorm 定稿

- **用户关键原话**:「把 query_faq 的内部实现从关键词查表升级成向量语义检索,工具的入参出参契约保持不变」「政策手册这类没有天然问题的,questions 填所在章节标题、category 填上级标题路径」「先写 MySQL 记『待向量化』,再写 Milvus 拿 vector_id 回填、状态转『已向量化』;按主键幂等,挂了能重跑」「本章只跑 dense 向量单路」;拍板时贴出硅基流动 key:「这是我硅基流动的API key……你看看是不是从中可以获得 bge-M3 模型」,澄清时补定位:「这一章只跑通『向量检索』这一条路径,让 query_faq 先能查着,更精细的检索优化留到下一章」;并随消息附上 knowledge_chunks / qa_extraction_staging 两表完整 DDL(用户自写,含 utf8mb4 防双层编码的 SET NAMES)。
- **关键产出**:环境侦察四条硬事实——①无 GPU、内存 7.4GB;②HuggingFace / PyPI / pytorch CPU 源可达;③Docker Hub `registry-1.docker.io` 直连 i/o timeout 且无镜像加速,`milvusdb/milvus` 镜像 manifest 与真实 pull 双双实测失败;④main 停在 ch02 规划提交,ch03 基于 ch02 分支叠加。硅基流动 key 实测通过:BGE-M3 批量入参、1024 维、「邮费是多少」vs「运费怎么算」余弦 0.78(低于去重阈值 0.88,换说法不会误判重复)。四项拍板:**BGE-M3 走硅基流动 API**、**挖知识 = 独立批处理脚本 + crontab 示例**、**防幻觉约束纳入本章**(ch02 实测模型把空结果编成「满 99 包邮」,留账在案)、**Milvus Lite**。设计文档 `docs/superpowers/specs/2026-09-05-ch03-rag-knowledge-base-design.md`(commit d6c4938,拍板后回填)。
- **拒绝或纠偏**:用户第一轮把硅基流动 key 贴进了「Milvus 形态」题(自述「好像没太理解你的意思,可能答得驴头不对马嘴」),主动喊停要求重问;澄清后明确选 Milvus Lite。此前异步等待期间 spec 曾按推荐项预写一版(嵌入原定本地 sentence-transformers),拍板后回填为 API 方案,同步修订 §2/§9。
- **翻车与返工**:spec 预写后回填一次(用户拍板晚于初稿,轻度返工,无代码损失);其余无。

## 2026-09-05 · 计划评审通过与执行方式

- **用户关键原话**:确认执行方式时选「Inline 执行(推荐,同 ch02)」(此前中途回归喊停,把四项拍板重新问过一遍,拍板齐全后才放行计划)。
- **关键产出**:实现计划 `docs/superpowers/plans/2026-09-05-ch03-rag-knowledge-base.md`(commit eceab2b),12 任务:TDD 主线为切分器→嵌入客户端→Milvus 封装→双写持久层,数据类任务(知识文档、评估集)按工作约定以断言/评估集验证代替红绿;硅基流动 key 已实测入库 `.env`(gitignore),`.env.example` 仅占位符。
- **拒绝或纠偏**:无;用户对计划直接放行。
- **翻车与返工**:无。


## 2026-09-05 · Task 1(双表 DDL + ORM + config)

- **关键产出**:Context7 核对 pymilvus 3.0.1:快捷建集合 `create_collection(name, dimension, primary_field_name="id", id_type="int", auto_id=False, metric_type="COSINE")` 恰合需求(自定主键=MySQL chunk id);`upsert(name, data=[{"id","vector"}])`、`search(name, data=[vec], limit=k)`(hit 带 id/distance)、`delete(name, ids=…)`、`get_collection_stats()["row_count"]`。依赖 pymilvus 3.0.1 + milvus-lite 3.2.1 装妥。用户 DDL 原文进 `db/init.sql`(SET NAMES 不重复);ORM 两模型(BigInteger→Integer 变体);config 九项。
- **拒绝或纠偏**:无。
- **翻车与返工**:转录型任务(DDL/配置字段)实现先于测试跑,红阶段被压缩——纪律偏差,自我记录;后续逻辑任务严格先红后绿。

## 2026-09-05 · Task 2(结构感知切分器)

- **关键产出**:`app/chunking.py` 六规则全实现并六组用例通过——标题层级/section_path、faq 的 category(取 H2 分组)与 questions(标题+「其他问法:」行剥离换行并入)、policy 的 questions=节标题/category=上级路径、超长递归(段落→句读→硬切)、相邻块重叠(尾部窗口裁到最近句号后,窗口内无句读则不重叠)、表格按行切每块复制表头+分隔行、is_key_clause 关键词启发。
- **拒绝或纠偏**:无。
- **翻车与返工**:`_parse_sections` 初版漏了把新标题压栈(只更新 current),导致 H2 的祖先恒为空、category 全部落到自身——红阶段测试精准命中,压栈一行修复后全绿。TDD 红绿先行的价值实锤。

## 2026-09-05 · Task 3(嵌入客户端 + FakeEmbedding)

- **关键产出**:`app/embedding.py` SiliconFlowEmbedder(httpx 直连 /embeddings,batch_size 切批、index 排序保序、非 200 收敛 RuntimeError,transport 可注入 MockTransport);`tests/helpers.py` 新增 FakeEmbedding(近义词归一 + 词库锚定,换说法 cos=1.0)与公共 `cosine()`;六用例全绿。
- **拒绝或纠偏**:计划里 FakeEmbedding 初稿(二字滑窗 + 随机向量)推演即发现换说法相似度只有 ~0.33,撑不住 >0.9 断言——改为词库锚定设计(领域词才产分量)。
- **翻车与返工**:token 伪随机向量初版用 64 位 seed 按 3bit 步长采 64 维,j≥22 后全是常数尾,异话题向量基线重合 cos=0.87;测试当场命中,换 sha512 全 64 字节展开修复。

## 2026-09-05 · Task 4(Milvus Lite 集合封装)

- **关键产出**:`app/vector_store.py` KnowledgeVectorStore——构造零 I/O(client 惰性)、快捷建集合(id INT64 主键 auto_id=False + FLOAT_VECTOR COSINE)、upsert 主键幂等、search 返回 [(id, 相似度)] 降序、集合不存在时 search/delete/count 静默收敛;真 milvus-lite tmp 文件六用例全绿。
- **拒绝或纠偏**:无。
- **翻车与返工**:Context7 通用文档只说 COSINE「distance 越小越像」,真库实测 hit["distance"] 直接就是相似度(同向量 1.0、正交 0.0),我按「1-distance」换算把自命中打成了 0 分——roundtrip 用例当场红,修正为直接取 distance 并把实测结论写回代码注释。真库测试不可省的实证。

## 2026-09-05 · Task 5(MySQL 持久层 + 向量化循环)

- **关键产出**:`app/kb.py` KnowledgeBaseStore(按文档重建幂等 replace_doc_chunks / pending 扫描 / mark done 回填 vector_id / 三格 vector_text / staging 全套流转 / conversation_ids_with_user_talks)+ vectorize_pending(批式:embed→upsert→mark,成功一批记一批,max_chunks 模拟中断);七用例全绿,全量 84 passed 1 xfailed 无回归。
- **拒绝或纠偏**:测试初版断言「重跑后新旧 id 不相交」——SQLite 删行后 rowid 复用导致假红;真不变量是「新旧 id 集合相等」且 id 相交对 upsert 幂等无影响,修正断言并注释 MySQL/SQLite 差异。
- **翻车与返工**:无(mark_vectorized 初稿先 bulk 置空再逐行回填,写实现时当场简化,未到测试环节)。

## 2026-09-05 · Task 6(知识文档种子)

- **关键产出**:`knowledge/` 三件——退货政策.md(「邮费与运费」节:8 元/满 99 包邮/质量退货商家承担,验收 1 靶点)、商品FAQ.md(ch02 八条 FAQ 等价 + 「其他问法」别名行)、售后手册.md(28 行运费标准表触发表格按行切、800+ 字退货处理细则触发递归切)。六条切分断言一次全绿。
- **拒绝或纠偏**:无。
- **翻车与返工**:无。

## 2026-09-05 · Task 7(建库管道)

- **关键产出**:`scripts/build_kb.py`——全量建库(逐文档切分→重建→删旧向量→统一向量化)、`--max-chunks N` 模拟中断、`--resume-only` 续跑(不动文档只补 pending)、统计输出;三用例全绿(全量幂等/中断续跑/块粒度参数)。
- **拒绝或纠偏**:无。
- **翻车与返工**:红阶段揪出真实设计缺口——`build` 重跑会先全量重建文档,把「中断续跑」变成「全部重来」(19 块全部重新向量化)。按 DDL「按主键幂等,挂了能重跑」的精神补 `rebuild_docs=False` 的续跑路径,验收叙事回到「漏向量化的块被捡起补齐」。

## 2026-09-05 · Task 8(挖知识管道 + 历史会话种子)

- **关键产出**:`scripts/mine_qa.py`(选料跳过已挖会话/纯工具轨迹与无 user 消息的会话不可挖、batch_size 分批、structured output 抽 MinedQAList、暂存表全量 extracted → 「库内已有知识」「批内已保留项」双路余弦去重 → kept 入库 discarded 留痕 → 可选向量化收尾);`db/seed_conversations.py` 四通与知识文档互补的历史对话;`cosine` 提升为 app/embedding.py 公共工具;四用例全绿,全量 97 passed。
- **拒绝或纠偏**:无。
- **翻车与返工**:清理 tests/helpers.py imports 时误删 `math`,FakeEmbedding 三连红 NameError,当场补回。

## 2026-09-05 · Task 9(query_faq 换核 + 防幻觉 + 装配)

- **关键产出**:`build_tools(session_factory, embedder=None, vectors=None, top_k=None)`(缺省按 Settings 构造真实现);query_faq 内核换为 embed → Milvus Top-K → MySQL 回取,questions 首行充当 question 字段,异常收敛 `{"items": []}`,契约不变;prompts 加防幻觉硬约束(「严禁编造政策、价格、时效或任何承诺」);main.py 装配真 embedder/向量库,conftest 全替身化。全量 100 passed 1 xfailed。
- **拒绝或纠偏**:契约测试发现「量子力学」漏网——向量检索天然返回 Top-K 不设下限,零相关命中也进 items;加 RETRIEVAL_SCORE_FLOOR=0.5 相似度下限,与 prompt 硬约束构成双保险(ch02 的「查无结果编造」账就此收清)。
- **翻车与返工**:无。

## 2026-09-05 · Task 10(检索评估集 + 真跑)

- **关键产出**:`tests/eval/retrieval_samples.jsonl` 12 条标注样例(换说法为主,「邮费是多少」领衔)+ `scripts/eval_retrieval.py`(真 MySQL + 真 Milvus Lite + 真 BGE-M3,hit@top_k 报告)。真跑 **hit@3 = 12/12**,「邮费是多少」Top-1 命中退货政策运费说明(相似度 0.706),ch02 漏召回场景翻案。
- **拒绝或纠偏**:首轮 10/12,两个 ❌(「快递费怎么算」「寄到偏远地区要加钱吗」)复盘是评估口径太窄——召回的售后手册运费标准表本就是正确答案,却把期望钉死在退货政策.md;放宽为多文档期望(允许 str|list),不是检索的锅不改检索。
- **翻车与返工**:两处环境实证——①milvus-lite 要求库文件父目录先存在(报 `dir: data not exists`),vector_store 惰性连接时补 makedirs;②跨进程重开文件库集合处于 released 态,search 前需幂等 load_collection。均已修复并回归。
