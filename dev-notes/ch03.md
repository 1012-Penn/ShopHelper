# ch03 开发留痕:RAG 基础——知识库 + 向量检索(query_faq 内核升级)

> 工作约定:每完成一个阶段追记一段,每段记四样:用户关键原话、关键产出、拒绝或纠偏、翻车与返工。

---

## 2026-09-05 · Brainstorm 定稿

- **用户关键原话**:「把 query_faq 的内部实现从关键词查表升级成向量语义检索,工具的入参出参契约保持不变」「政策手册这类没有天然问题的,questions 填所在章节标题、category 填上级标题路径」「MySQL 建 knowledge_chunks 表当原文权威源、Milvus 建 knowledge 集合;先写 MySQL 记『待向量化』,再写 Milvus 拿 vector_id 回填、状态转『已向量化』;按主键幂等,挂了能重跑」「本章只跑 dense 向量单路」;并随消息附上 knowledge_chunks / qa_extraction_staging 两表完整 DDL(用户自写,含 utf8mb4 防双层编码的 SET NAMES)。
- **关键产出**:环境侦察四条硬事实——①无 GPU、内存 7.4GB;②HuggingFace / PyPI / pytorch CPU 源可达,本地跑 BGE-M3 可行(首次下载约 2.3GB);③Docker Hub `registry-1.docker.io` 直连 i/o timeout 且无镜像加速,`milvusdb/milvus` 镜像 manifest 与真实 pull 双双实测失败;④main 停在 ch02 规划提交,ch03 基于 ch02 分支叠加。四项决策向用户提问未获即时应答(异步模式),按系统指引采纳推荐项并在此显著留痕供 veto:
  1. **Milvus 形态 = Milvus Lite**(pip 内嵌,官方 `MilvusClient` API,与 Standalone 同 API,上生产只改连接串;Docker Hub 实锤拉不到);
  2. **BGE-M3 运行 = 本地 sentence-transformers + torch CPU 版**(单条编码约 1-2s,演示规模够用);
  3. **挖知识定时任务 = 独立批处理脚本 `scripts/mine_qa.py` + crontab 外挂调度**(README 附示例行,中断重跑演示最直接);
  4. **防幻觉纳入本章**:query_faq 查无结果时 system prompt 硬约束「如实说明不许编」(ch02 实测模型把空结果编成「满 99 包邮」,留账在案)。
  设计文档 `docs/superpowers/specs/2026-09-05-ch03-rag-knowledge-base-design.md`。
- **拒绝或纠偏**:向用户发出四项决策提问(AskUserQuestion),未获应答;未擅自更换技术选型(BGE-M3 / Milvus / MySQL 权威源全部保持),仅定「运行形态」,并把四项默认列为本章最显眼的 veto 点。
- **翻车与返工**:无。
