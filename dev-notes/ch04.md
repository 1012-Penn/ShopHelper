# ch04 开发留痕:RAG 进阶——混合检索 + 重排 + 评估体系

> 工作约定:每完成一个阶段追记一段,每段记四样:用户关键原话、关键产出、拒绝或纠偏、翻车与返工。

---

## 2026-09-06 · Brainstorm 定稿

- **用户关键原话**:需求书定调「混合检索加重排,再配一套评估体系」——「关键词召回这条路也落在 Milvus 上,用它 2.5 版起原生的 BM25 全文检索,text 字段挂 BM25 函数,中文分词用内置 chinese analyzer;检索时 dense 向量和 BM25 各召回 Top-50,用 hybrid_search 配 RRF 融合」「用 bge-reranker-v2-m3 精排出 Top-10;组装进 prompt 时把最相关的放首尾,别按分数顺序堆中间」;引用编号要「能映射回来源 chunk 的章节路径、跳回原文」,「检索不到或证据置信度低就显式拒答,不硬编答案」;并随消息附上 low_confidence_questions / faith_cases 两表完整 DDL(用户自写,注释里已定死 faith_cases 一题一行/seen_count 跨轮/复发退回未解决/citations 存证据全集的台账语义,以及评估集四桶名 A_policy / B_model / C_colloquial / E_multi);工作要求强调「全程走 Superpowers 流程」「涉及具体库一律先 Context7 查文档」「技术选型定死,走不通停下来问」。
- **关键产出**:可行性侦察三条硬事实——①Context7 核对 Milvus BM25 Function/hybrid_search API 后,**真库探针全绿**:milvus-lite 3.2.1 支持本章全部所需特性(BM25 建集合、jieba 分词、hybrid_search RRF、两侧 expr 过滤、upsert 幂等),不需要 Standalone;②文档的 `chinese` analyzer 在 Lite 不存在(报错明示 supported: standard/jieba),等价配置 `{"tokenizer": "jieba"}`,补装 jieba 0.42.1;③硅基流动 `/v1/rerank` 用现有 EMBEDDING_API_KEY 实测通过(BAAI/bge-reranker-v2-m3,区分度 0.993/0.204/0.0003)。四问拍板:**Reranker=硅基流动 API**、**评估集=四桶按解读(政策/型号/口语/多约束)每桶 8 题共 32 题,D 缺号即无 D 桶**、**落池=拒答标记检测(【无法回答】开头,路由层检测)**、**query_faq 契约演进放行**(出参演 n/chunk_id/section_path、入参加可选 category、SSE 加 citations 帧)。设计文档 `docs/superpowers/specs/2026-09-06-ch04-rag-advanced-design.md`(commit 见 git log),两个默认边界随设计批准生效:不引入 Langfuse、query 改写走 LLM 且可开关。
- **拒绝或纠偏**:无(四问均按推荐项通过;设计整体一次批准)。
- **翻车与返工**:探针两连翻车当场修正——①`analyzer_params={"type":"chinese"}` 报 unknown tokenizer(Lite 只认 standard/jieba);②`MilvusClient.search` 传 `param` 撞 `got multiple values for keyword argument`(该用 `search_params`)。两条都按「真库为准」回填进 spec §2。

## 2026-09-06 · 计划评审通过与执行方式

- **用户关键原话**:计划评审问题下直接选「通过,Inline 执行(推荐)」(同 ch02/ch03 惯例,本会话逐任务执行,每任务末全量回归 + dev-notes 追记 + commit)。
- **关键产出**:实现计划 `docs/superpowers/plans/2026-09-06-ch04-rag-advanced.md`(commit 764e257),14 任务:双表 DDL/ORM/config → Milvus v2 集合封装(含 kb 双写适配与 FakeVectorStore 升级)→ rerank 客户端 → query 改写器 → 检索策略模块(反漏斗/低置信/过滤回退)→ 质量台账持久层 → query_faq v3 → prompts 三件套 → chat 路由 citations 帧+落池 → 型号文档+32 题评估集 → 检索四策略评估 → 忠实度评估 → 前端(Vibe,例外)→ 收口真机验收。计划自审当场修四处:①删 `rerank_top_n`(与 `retrieval_final_top_k` 冗余);②`build_tools` 改「只有生产路径才构造真 rewriter/reranker」,测试部分注入不再误出网;③Task 9 拒答用例先灌知识,防检索低置信与自评拒答双入口同时触发;④FakeReranker 打分从「doc 覆盖率」改「query 覆盖率」,短问法命中关键词即高分,避免 0.30 阈值误杀正常轮。
- **拒绝或纠偏**:无;计划一次放行。
- **翻车与返工**:无(上述四处属计划内自审修正,未到实现阶段);留痕编辑自己翻了一次车——追记时误吞了 Brainstorm 段标题导致两段顺序颠倒,当场发现重排修复。

## 2026-09-06 · Task 1(双表 DDL + ORM + config + .env.example)

- **关键产出**:用户两表 DDL 原文追加进 `db/init.sql`(low_confidence_questions 挂 conversations FK、faith_cases 一题一行 uk_eval_id + 复发语义注释全保留);ORM 两模型(LowConfidenceQuestion / FaithCase,BigInteger→Integer 变体承载 SQLite);config 七项(rerank_api_base/rerank_api_key/rerank_model/rerank_score_floor=0.30/hybrid_candidates=50/retrieval_final_top_k=10/query_rewrite_enabled);`.env.example` 加 RERANK_* 占位(RERANK_API_KEY 留空回落 EMBEDDING_API_KEY)。红绿:先写 roundtrip/默认值测试确认 ImportError,再实现,`tests/test_models.py + test_config.py` 9 passed;全量 **104 passed, 1 xfailed**。
- **拒绝或纠偏**:无。
- **翻车与返工**:无。

## 2026-09-06 · Task 2(Milvus 集合 v2 封装)

- **关键产出**:`app/vector_store.py` 整体重写——v2 schema(id 主键 + dense COSINE + text(jieba,BM25 源)+ sparse(BM25 Function 产出)+ category 过滤字段),`upsert(rows)` 带 text/category、`dense_search` / `bm25_search` / `hybrid_search(RRFRanker)` 三路检索全支持 `filter_expr`(MilvusClient 用 `filter`、AnnSearchRequest 用 `expr`,探针实锤),旧 schema 集合 ensure 时自动 drop 重建,`search()` 留 ch03 兼容壳(Task 7 移除);`kb.vectorize_pending` upsert 改带三格文本与 category;FakeVectorStore 升 v2 接口(expr 仿真 category 匹配、BM25 负分仿真);pyproject 声明 jieba==0.42.1。真 milvus-lite tmp 库 12 用例全绿:型号「SH-E300」纯 BM25 命中、hybrid RRF 双路提名居首、expr 两侧过滤、旧库重建、构造零 I/O。全量 **110 passed, 1 xfailed**。
- **拒绝或纠偏**:无。
- **翻车与返工**:无(探针阶段已把 analyzer/search_params 两坑踩平,本任务一次绿;Context7 与真库核对结论直接写进模块 docstring)。
