# ch01 开发留痕:纯对话(流式多轮 + Prompt 模板 + 结构化抽取 + 历史裁剪)

> 工作约定:每完成一个阶段(brainstorm 定稿、计划评审通过、每个任务完成、code review 结论、finish)追记一段,每段记四样:用户关键原话、关键产出、拒绝或纠偏、翻车与返工。

---

## 阶段 1:brainstorm 定稿(2026-09-04)

**用户关键原话:**

- "我要用 Superpowers 模式做一个电商智能客服系统,第一步先跑通纯对话。"(附四条功能需求:多轮对话 SSE 逐 token;PromptTemplate 模板化 + 客服角色约束;with_structured_output 抽取订单号/诉求类型/期望方案;历史裁剪 + token 预算。验收三条:curl 见流式、两轮接上下文、售后描述出 JSON。)
- "这是我的deepseek key"(key 已当场 curl 验证连通,上游实际型号 deepseek-v4-flash;key 本身不落入任何文档,只进 .env)
- 会话管理形态选择:"服务端内存会话(推荐)"
- 聊天页范围:"本章就带最小聊天页"(走 Vibe Coding 例外,不套 brainstorm/TDD/review)
- 模型接入方案:"A: ChatOpenAI 统一接入(推荐)"(换 GPT/Claude 网关/DeepSeek/Ollama 只改 .env)
- 设计四节评审:"都可以,定稿"

**关键产出:**

- 设计 spec:`docs/superpowers/specs/2026-09-04-ch01-pure-chat-design.md`(含 API 契约、SSE 事件协议、裁剪算法、评估集策略、风险对策、决策记录)
- 环境结论:本机无 Ollama、环境变量无 key;Python 3.10.12 + pip3;DeepSeek 上游连通

**拒绝或纠偏:** 无(四个选择题全部采纳推荐项)。

**翻车与返工:** 上一个会话在产出任何文件之前中断(当时仓库为空、无 dev-notes),本次从零重走 brainstorm;无代码返工。另:用户 key 直接贴进了聊天记录,已提示后续可轮换。
