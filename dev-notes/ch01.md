# ch01 开发留痕:纯对话(流式多轮 + Prompt 模板 + 结构化抽取 + 历史裁剪)

> 工作约定:每完成一个阶段(brainstorm 定稿、计划评审通过、每个任务完成、code review 结论、finish)追记一段,每段记四样:用户关键原话、关键产出、拒绝或纠偏、翻车与返工。

---

## 阶段 1:brainstorm 定稿(2026-09-04)

**用户关键原话:**

- "我要用 Superpowers 模式做一个电商智能客服系统,第一步先跑通纯对话。"(附四条功能需求:多轮对话 SSE 逐 token;PromptTemplate 模板化 + 客服角色约束;with_structured_output 抽取订单号/诉求类型/期望方案;历史裁剪 + token 预算。验收三条:curl 见流式、两轮接上下文、售后描述出 JSON。)
- "这是我的deepseek key"(key 已当场 curl 验证连通,上游实际型号 deepseek-v4-flash;key 本身不落入任何文档,只进 .env)
- 会话管理形态选择:"服务端内存会话(推荐)"
- 聊天页范围:"本章就带最小聊天页"(走 Vibe Coding 例外,不套 brainstorm/TDD/review)
- 模型接入方案:"A: ChatOpenAI 统一接入(推荐)"(换 GPT/Claude 网关/DeepSeek/Ollama 只改 .env)
- 设计四节评审:"都可以,定稿"

**关键产出:**

- 设计 spec:`docs/superpowers/specs/2026-09-04-ch01-pure-chat-design.md`(含 API 契约、SSE 事件协议、裁剪算法、评估集策略、风险对策、决策记录)
- 环境结论:本机无 Ollama、环境变量无 key;Python 3.10.12 + pip3;DeepSeek 上游连通

**拒绝或纠偏:** 无(四个选择题全部采纳推荐项)。

**翻车与返工:** 上一个会话在产出任何文件之前中断(当时仓库为空、无 dev-notes),本次从零重走 brainstorm;无代码返工。另:用户 key 直接贴进了聊天记录,已提示后续可轮换。

---

## 阶段 2:计划前期准备——Context7 MCP 修复(2026-09-04,会话即将重启)

**用户关键原话:**

- (对我"本会话没有 Context7 工具、改用 venv 实装+官方文档"的说明)："Context7 MCP 工具 请你先配置一下这个工具."——并拒绝了 `apt-get install python3.10-venv`,要求先修 Context7 再继续。

**关键产出:**

- 排查结论(走 zcode-guide:diagnosing-mcp 流程):`~/.zcode/cli/config.json` 的 context7 配置本身规范、JSON 合法、无遮蔽;手动拉起 `npx -y @upstash/context7-mcp` 实测 2.9s 完成 JSON-RPC 握手,`tools/list` 返回 `resolve-library-id` / `get-library-docs`(Context7 v4.0.4),stderr 干净。**根因:会话启动时未连上,运行中会话不会中途注入 MCP 工具;修法 = 重启会话。**
- 用户选择:重启会话使其生效。**下个会话第一件事:确认 `mcp__context7__*` 工具存在,再继续写实现计划。**

**拒绝或纠偏:** 用户纠偏了顺序——不许"先用替代方案凑合",坚持先配好 Context7(符合工作要求 #3)。

**翻车与返工:** 本会话自始至终无 `mcp__context7__*` 工具(无法自愈,只能重启);`apt-get install python3.10-venv` 被用户拒绝,venv 创建被搁置。

**下个会话接续点(照此继续):**

1. 验证 `mcp__context7__*` 工具已注入;
2. 建环境:python3.10-venv 的 apt 安装曾被拒,需重新请求放行,或改用 `pip3 install --user virtualenv` 方案;
3. Context7 查 LangChain(langchain-core / langchain-openai 当前版本)与 FastAPI SSE 的官方用法;
4. 写实现计划到 `docs/superpowers/plans/`,自审后交用户评审(计划评审通过后记入本文件);
5. 执行计划:可单测代码走 TDD,纯 Prompt 走标注样例评估集,聊天页 Vibe Coding;最后验收三条 curl + code review + finish。

---

## 阶段 3:实现计划定稿(2026-09-04)

**用户关键原话:**

- "继续"(按上一会话接续点顺序执行,本阶段无新的设计决策)

**关键产出:**

- 接续点 1–3 全部完成:
  1. `mcp__context7__*` 工具确认已注入,查询正常;
  2. `.venv` 按备选方案重建:`pip3 install --user virtualenv`(21.7.8)+ `python3 -m virtualenv .venv`,自带 pip;装齐并锁定版本:fastapi 0.141.1、uvicorn 0.52.4、langchain 1.4.0、langchain-openai 1.6.0(langchain-core 1.6.1)、pydantic-settings 2.15.0、pytest 9.1.1、pytest-asyncio 1.4.0、httpx 0.28.1;
  3. Context7 官方文档 + 本地 venv 实测双重核查 LangChain 1.x / FastAPI 用法。
- 实现计划:`docs/superpowers/plans/2026-09-04-ch01-pure-chat.md`(10 个任务,步进到"写失败测试→跑→实现→跑→commit"粒度,含"实现前 API 核查结论"一节)。
- API 核查要点:`GenericFakeChatModel` 1.x 构造参数是 `messages=iter([...])`(不是 `responses`),迭代器一次性消耗,每个测试须新建实例;`astream` 产出 chunk 用 `.text` 属性,按空格切词;`with_structured_output` 采用 Pydantic + 中文 Field 描述 + `method="function_calling"`;FastAPI 0.141 新增官方 `fastapi.sse.EventSourceResponse`,但定稿契约是 JSON 内嵌 type 的单 `data:` 帧,维持手写方案,新类仅记录备查。

**拒绝或纠偏:** 无用户纠偏。计划自审发现并修正一处设计矛盾:chat 路由原稿把不含 System 的会话历史直接传给 `trim_history`,与其"messages[0] 视为 System 永不裁剪"契约冲突(第一条用户消息会被误当 System);改为 System 拼在 [0] 一起裁剪,并补了 prompt 构建纯函数的测试。

**翻车与返工:** 无代码返工(计划阶段);上阶段被拒的 `apt-get install python3.10-venv` 本阶段未再触碰,直接走 virtualenv 备选方案绕开阻塞。

**当前状态:计划自审完成,待用户评审;评审通过后按计划执行(TDD + 评估集 + 聊天页 Vibe Coding)。**

---

## 阶段 4:计划执行 + 任务级/终审 code review(2026-09-04)

**用户关键原话:**

- (问)选方式一的话是开计划模式还是普通模式?(答:普通模式即可,计划已在仓库)(选)："行吧,选方式,一开始执行."——计划评审通过,Subagent-Driven 方式执行。

**关键产出:**

- 分支 `ch01-pure-chat`(cc4d700..302a6df,12 commits),10 任务全部完成,每任务独立实现者子代理 + 独立评审子代理:
  - T1 骨架配置 / T2 历史裁剪(10 测)/ T3 内存会话 / T4 schemas+prompts / T5 /api/chat SSE+应用工厂 / T6 会话历史 / T7 /api/extract / T8 聊天页(Vibe Coding,用户豁免 review)/ T9 评估集+真上游评估(首跑 order_no 8/8、issue_type 8/8)/ T10 验收。
- 全量测试 38 passed + 1 xfailed,0 warnings;真上游三条验收 curl 全过(逐帧流式、两轮上下文接住、抽取合规 JSON);GET / 200。
- 终审(整分支)判定 With fixes → 一个修复子代理补齐:I-1 prompt 组装端到端测试(RecordingChatModel)、I-2 README 交付说明、断开不落库用例;范围重审三项全 ADDRESSED、无新破坏。

**拒绝或纠偏:** 无用户纠偏。控制器裁定 7 条(全记录于执行 ledger):①in-place 分支代替 worktree;②平台无 model 参数、子代理继承会话模型;③dev-notes 与 ledger 并行;④T2 计划"9 passed"笔误以代码 10 为准;⑤T5 模块级 app 读 .env 不可移植——conftest setdefault 兜底修(产品代码不动);⑥T6/T7 计划预测 RED 形态作废以实际为准;⑦T9 评估样例 #3"运单号"标 order_no 属未声明语义约定,挂起待用户裁决(修法属 spec 层:改样例或 Field 描述加"含运单号",需重烧真上游评估)。

**翻车与返工:**

- T1 首个实现者子代理 600s 无响应失联(留合法 RED 期产物),二次派发核验续作,无返工;
- 验收 2 首跑"没接住上下文"是控制器自己的验收命令 `head -30` 掐断流所致——客户端断开不落库正是 spec 设计行为,非缺陷,完整收流后重跑即过;
- "客户端断开不落库"端到端用例受 httpx ASGITransport 全量缓冲限制(取消无法传播到 app),按预案以完整断言 + `xfail(strict=True)` 落账:传输层将来支持时自动 XPASS 逼人摘牌;真实 uvicorn 下该性质已由终审人工确认成立;
- 终审另记入 ch02 待办:extract 422/500 分流、error 帧透传 str(exc) 改造、SessionStore 上限、trim 成对裁剪、并发测试重写触发条件。

---

## 阶段 5:finish(2026-09-04)

**用户关键原话:**

- (给 SSH 地址)"git@github.com:1012-Penn/ShopHelper.git 提交到我的GitHub,这个是SSH的连接.";(选)："你本地合并再推,"

**关键产出:**

- 推送前安全检查:全部历史无 key 泄漏("sk-"命中均为 "task-" 类单词子串),.env 与 .superpowers 草稿区未被追踪;
- `origin` = git@github.com:1012-Penn/ShopHelper.git,main 与 ch01-pure-chat 双分支推送成功;
- 用户选本地合并:fast-forward 合并(cc4d700..80d7928),**合并结果上实测 38 passed + 1 xfailed、0 warnings**,推回 origin/main;分支删除(本地+远端),SDD 草稿区清理,git 历史即完整记录。

**拒绝或纠偏:** 无。

**翻车与返工:** 无。

**ch01 至此收口。** 已知挂起:评估样例 #3"运单号"语义约定待用户裁决(见阶段 4 裁定⑦);ch02 待办清单见阶段 4 末条。
