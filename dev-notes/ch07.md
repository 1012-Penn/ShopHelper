# ch07 开发过程留痕(会话上下文管理:三层历史 / 后台异步摘要 / 多会话前端)

## 2026-09-08 · brainstorm 定稿

- **用户关键原话**:需求全文见会话(三层结构/摘要后台异步/拼装顺序/token 预算从窗口倒推/State 贯穿/可观测/多会话侧栏;「只管当前会话,不跨会话、不存用户画像」「降级只挪这两个 id、不搬数据」「梗概一段段攒、压完不回头重写,旧梗概只作背景给模型看、不参与合并」「梗概不许单独占一条 system,上游模板会把所有 system 上提合并渲染」「装得下就不压,压缩是成本不是美德」「中文按字数折 token 的口径要和预算一起校准,只改一个净效果是反的」);DDL 两文件内容随需求给出。
- **关键产出**:
  - 分类为 Architectural 路径;现状摸底:历史裁剪在 `app/history.py::trim_history`(ch03,整条丢弃式)+ `chat.py` 路由层注入,State.messages 目前是「本轮落库 pending dict」非完整历史,`InMemorySaver` 只承载运行态;工具行全量落库(ch02 契约)。
  - Context7 + 已装版本内省(langgraph 1.2.11 / langchain-core 1.6.1)核对:`add_messages` 按 id 替换、缺 id 追加(`langgraph.graph.message`);`trim_messages(messages, *, max_tokens, token_counter, strategy='last', start_on, end_on, include_system)` 支持自定义 token_counter——层 1 装填用后者落地,技术栈点名兑现。
  - **预算公式逐位对齐验收数字**:滑窗=窗口−输出预留−(输入预留+步数×工具结果上限)−固定开销;演示配置下 18000−2000−(2000+3×1200)−(1800+5×250+200+1500)=18000−2000−5600−4750=**5650**;层1=int(5650×0.7)=int(3954.999…)=**3954**(Python 浮点语义,用户给的 3954 与此严丝合缝,说明参考实现就是 int() 截断),层2=**1695**。只调窗口不调其余 → 滑窗归零报「上下文预算不足」的验收陷阱也复核成立。※TDD 首跑逮住我一次口算错(18000−2000−5600−4350=6050≠5650,把 10400 当 10000),prompt 开销组件 1400→1800 校正后逐位命中——测试先行值回票价。
  - spec 落盘:`docs/superpowers/specs/2026-09-08-ch07-context-management-design.md`(自审过:占位符/一致性/歧义/范围;演示数字、锚点数学 `layer1_from=首条保留行id−1`、budget=0 行为、触发时机全部写死)。
- **自决决策点**(用户一次性给了完整规格未留问答往返,按 ch06「推荐项自决、全程留痕可纠偏」先例):①checkpointer 沿用 InMemorySaver 不引 SqliteSaver——持久真源归 MySQL,重启从 MySQL 回灌 State(带消息 id 防并发双灌),避免第二个会漂移的持久真源;②工具结果与纯工具调用消息不再落 messages 表(req 2 明示「工具结果压根不落消息表」),工具轨迹活在当轮 State/checkpoint,层 2 的工具行渲染规则对旧数据仍生效;③分层视图构建源=MySQL(锚点是 MySQL id,checkpoint 轮内工具消息没有也不可能有 MySQL id,位置映射不可靠),checkpoint 只承载图内完整轨迹,两套真源各走各的=req 5 字面语义;④`RERANK_TOP_K` 顶替 `retrieval_final_top_k` 全部职责(验收按此口径点名),旧 env 名退役;⑤摘要触发挂在每轮开头路由层(上一轮落库后的占用),级联降级同步、摘要投后台。
- **拒绝或纠偏**:无(用户规格即定稿;自决项如上,均可纠偏)。
- **翻车与返工**:无。一次前置排雷:最初想用「aget_state 判 checkpoint 空否」做回灌判定,核对后改进程内 hydrated 集合——少一个新 API 面,并发首轮靠 add_messages 按 id 去重兜住。

## 2026-09-08 · 计划评审通过 + 执行开始(inline)

- **用户关键原话**:「全程走 Superpowers 流程,技能自动触发」;「产出不是可单测代码的任务(纯 Prompt、数据类),把 TDD 那步换成拿标注样例或评估集跑一遍验证,其余步骤照走」。
- **关键产出**:`docs/superpowers/plans/2026-09-08-ch07-context-management.md`(10 任务:1 预算推导+配置改名退役,2 DDL/ORM/store,3 分层纯函数层,4 后台摘要服务,5 管线重接线,6 只读端点,7 前端侧栏(Vibe),8 真机验收,9 code review,10 finish)。自审:spec 各节均有任务对应、跨任务签名一致;两处测试草稿标注执行时写严格版(cascade noop 断言、layer2_tokens 直白循环)。
- **拒绝或纠偏**:writing-plans 默认推荐 subagent-driven;沿用 ch05/ch06 结论选 inline(executing-plans)——管线重接线与既有替身底座强耦合、共享上下文价值高,记为有意偏离,非用户否决。
- **翻车与返工**:无(计划期)。

## 2026-09-08 · Task 1 完成(预算推导+配置改名退役)

- **用户关键原话**:「token 预算从模型窗口倒推,不写死成常量」「历史预算取『想留住的轮数 × 每轮稳态占用』和『窗口实际匀得出来的』两个数里小的那个,层 1 拿七成、层 2 拿三成」「启动时自检一遍,连一轮都装不下就报警」。
- **关键产出**:`derive_budgets`/`ContextBudgets`(`app/context.py`);Settings 新增 ch07 十二个字段,`history_token_budget` 退役、`agent_max_steps`→`max_agent_steps`、`retrieval_final_top_k`→`rerank_top_k`(全部调用点同步);`.env.example` ch07 段。**TDD 首跑逮住口算错**:18000−2000−5600−4350=6050≠5650(把 10400 当 10000),prompt 开销 1400→1800 校正;再发现层 2 口径应为 `int(历史×0.3)` 而非 `历史−层1`(3954+1695=5649,留 1 token 余量)——spec/dev-notes/计划三处同步修正。全量 249 passed。
- **拒绝或纠偏**:无。
- **翻车与返工**:①上述口算错(测试先行的价值);②sed 批量改名把 `_settings(agent_max_steps=` 改出双括号 SyntaxError,读文件定点修复——教训:sed 模板要含右边界。

## 2026-09-08 · Task 2 完成(DDL/ORM/store)

- **用户关键原话**:「降级只挪这两个 id、不搬数据」「梗概单开一张会话摘要表,不塞进逐条消息里」(+用户随需求给出两个 SQL 文件全文)。
- **关键产出**:`db/ch07-summary.sql`/`db/ch07-layers.sql`(用户 DDL 原文落盘);`init.sql` conversations 三列 + conversation_summaries(全新卷一遍建全);ORM `Conversation` 三列 + `ConversationSummary`(uk_conv_seq);store 增 `get_rows(带id)/get_anchors/set_layer1_from/next_seq/append_summary(单事务:插段+推锚+重拼投影)/list_conversations`。6 项 store 测试;全量 255 passed。真机部署时老库需 apply 两个增量 SQL、新库 init.sql 直接覆盖。
- **拒绝或纠偏**:无。
- **翻车与返工**:store 忘导 `func`/`ConversationSummary` 两次 NameError,测试即逮即修。

## 2026-09-08 · Task 3 完成(分层纯函数层)

- **用户关键原话**:「最近的留原文,一个字都不压;中间一层按规则截短,用户原话不动、客服答复只留开头几十个字、大块工具结果换成一行标识;最远的异步压成一段段梗概」。
- **关键产出**:`app/context.py` 主体——`split_layers`(NULL 锚点语义)/`render_layer2`(三条半压规则,tool 残留渲染成 assistant 一行标识保协议合法)/`layer2_tokens`(渲染态口径)/`cascade_layer1`(trim_messages strategy=last+start_on=human 整轮切入;新锚点=首条保留行 id−1;trim 拒绝返回空=至少保最后一轮)/`build_layered`(超滑窗从最老端丢层2渲染块,仅本轮)/`render_history_text`/`build_background`/`truncate_tool_result`;`history.py` 增 `rows_to_messages`(带 id)。20 项测试。
- **拒绝或纠偏**:trim_history 退役挪到 Task 5 与消费者迁移同波执行(保持每个提交全绿),记为对计划文本的有意偏差。
- **翻车与返工**:**split_layers NULL 锚点哨兵写反**——`layer1_from=None` 用 `+inf` 作边界,在 `id > layer1_from` 语义下把全部行排出层 1(before=0 永不降级),测试逮住后改 `su` 兜底;同场修正自己对「降级方向」的误解:锚点向新方向单调右移(老内容从层 1 降入层 2),首版断言写反。

## 2026-09-08 · Task 4 完成(后台摘要服务)

- **用户关键原话**:「摘要提示词只提炼事实与诉求……对话里没出现的内容一个字不许编,寒暄闲聊不留,长度控制在几十到一两百字」「旧梗概只作背景给模型看、不参与合并,免得同一个事实被反复有损压缩」。
- **关键产出**:`SUMMARY_PROMPT`(四规则+旧梗概仅背景+几十到一两百字);`app/summarizer.py` `SummaryService`——`maybe_trigger`(同步返回,create_task 投后台;在飞集合防重入;任务引用挂 `_tasks` 防 GC)+ `_run`(批上界=触发时 layer1_from;空批 skip;start/done 留痕带覆盖区间与耗时;失败 warning 不上抛;超 300 字硬截断)。8 项测试。
- **拒绝或纠偏**:无。
- **翻车与返工**:测试断言踩两处小坑(日志断言用 `r.message` 拿到的是未插值格式串,应 `getMessage()`;prompt 文案含 `:\n` 换行断言漏写),修 断言不动实现。

## 2026-09-08 · Task 5+6 完成(管线重接线+只读端点)

- **用户关键原话**:「State 的 messages 字段挂 add_messages reducer,各节点只管吐新消息」「完整历史和精简版各走各的、互不影响」「system 人设、红线和工具定义打头,这一段每轮都一样……梗概不许单独占一条 system」「工具结果压根不落消息表」。
- **关键产出**:State.messages 挂 add_messages + `turn_user_msg_id`(log 节点据此切片,避免 turn_start_len 在去重场景错位)+ `layered`;initial_state 增 `history_rows` 回灌(带 MySQL id,并发首轮按 id 去重);六个节点消息产出全部 LC 化(agent 吐 AI/Tool,complaint/chitchat 吐 AI,ask_order/fallback 零吐 user);log 节点新落库契约(user + 含文本 assistant,纯工具调用行/tool 行不落);agent 纯静态 system + `build_background` 背景块(梗概+证据+订单+窄化,挂当前用户消息之后)+ 工具结果 TOOL_RESULT_MAX_TOKENS 截断 + 每步 model_ctx 原样日志;resolve 喂 layered.history_text;chat.py 轮前编排(降级→触发→装配→history_ctx,`prepare_turn`);main.py 挂 logs/app.log FileHandler + 预算启动自检(「上下文预算不足」ERROR 行)+ hydrated_threads + SummaryService;`GET /api/conversations`、`GET /api/conversations/{id}/messages`。`tests/test_ch07_pipeline.py` 9 项 + conversations 3 项;旧测试迁移 15 文件级(ch05/ch06 全部消息断言 LC 化、落库断言按新契约);全量 **293 passed, 1 xfailed**。
- **拒绝或纠偏**:回灌判定用进程内 hydrated 集合而非 `aget_state`(少一个 API 面,计划期已定);重写 agent.py 时一度漏 import SERVICE_PROMPT_TEMPLATE(NameError 即逮),无返工。
- **翻车与返工**:①log 节点单测期望写错——把「已回灌的旧历史」也当成本轮该落的行,修正期望(回灌行不重复落库);②迁移面 39 个红测全部按新契约迁移,零实现回退。

## 2026-09-08 · Task 7 完成(前端会话侧栏,Vibe 例外)

- **用户关键原话**:「聊天页加会话侧栏,列出该用户全部会话(新在前、首问预览、已摘要标记),点击切换并回载历史原文继续聊;『新对话』只是开新会话,旧会话仍在侧栏可切回」「侧栏加载失败静默降级不影响聊天」。
- **关键产出**:static/index.html——#main 布局包一层(侧栏 + 聊天列);`loadConversations`(失败隐藏侧栏)/`renderConvList`(高亮当前、摘要徽标、预览省略)/`switchConversation`(回载渲染气泡,失败保留当前会话);发送 done 后静默刷新侧栏;「新对话」清高亮不清列表;15s 轮询兜底刷新。node --check 语法过;效果终验随 Task 8 浏览器自动化。

## 2026-09-08 · Task 8 完成(真机验收五条,docker MySQL + 真模型全链)

- **用户关键原话**:「连续聊二十轮以上,token 不爆、系统不崩」「这时再问『最开始那个订单后来怎么说』,要能靠梗概里存下的订单号和诉求答对」「默认窗口下聊二十轮,不该触发任何降级和摘要。装得下就不压,压缩是成本不是美德」「grep model_ctx / history_ctx 能直接看到每轮发给模型的摘要和滑窗内容」。
- **环境**:docker compose down -v 重建(init.sql 含 conversation_summaries 与 conversations 三列)→ seed/build_kb(25 chunk)→ uvicorn 后台任务方式(ch06 教训)。上游 bigmodel 限流 1302(ch06 同款),按先例切 .env 注释内 DeepSeek 备用 key 环境变量覆盖,不动 .env 文件。**新坑**:本机代理(127.0.0.1:7897)劫持 milvus-lite 的 gRPC localhost 流量被 GOAWAY 掐断致检索挂起,服务启动加 `NO_PROXY=127.0.0.1,localhost` 解决。
- **关键产出(五条全过,证据 `reports/ch07-acceptance.md`)**:
  1. 场景 A(默认配置)23 轮零错误、零降级、零摘要,启动行 `[ctx] budget 窗口=65536 …滑窗=43536 层1=5250 层2=2250`;
  2. 场景 B(演示配置)启动行精确打出 `滑窗=5650 层1=3954 层2=1695`;填充至第 20 轮 `层1 降级`→`summary trigger 层2 约 1799 token > 预算 1695`→`summary done 第1段 覆盖=[214,242] 耗时 1.9s 字数 247`;回忆轮答复报出「订单1001(无线耳机,299元)已签收」并点出「催发货诉求尚未落实」;回忆轮 history_ctx 显示 摘要=1段、层2 从 1799 塌缩到 139(摘要吞掉整段);
  3. 场景 trap:仅 MODEL_CONTEXT_WINDOW=18000 → `上下文预算不足:滑窗 0 装不下稳态一轮 250`;
  4. 非阻塞证据:触发行 00:56:33.686、摘要完成 00:56:35.572,均早于触发轮流式结束(触发轮整体 7.3s);model_ctx/history_ctx 全程可 grep;
  5. 场景 S(HTTP 级):新在前/首问预览/回载 user-assistant 交替/切回续聊不串话题/摘要标记 3 条;浏览器视觉终验留用户本地(沙箱无浏览器后端,web-gui-tester 已尝试不可用)。
- **拒绝或纠偏**:无。
- **翻车与返工**:①验收脚本三连修:httpx 缺 base_url、启动行早于脚本启动被增量 LogWatch 错过(改全文件扫)、`summary done` 不是日志行的连续子串(实际格式 `[summary] session=N done 第M段`);②**场景 S 逮住真契约缺口**:ReAct 中间文本碎片(「我先查一下」)与熔断回溯答复都落库,回载出现两条 assistant——修复:agent 熔断不再重复追加 + log 节点只落最后一条含文本 assistant(中间碎片不算对话原文);修后全量 293 passed,场景 S 复跑通过。场景 B 证据产生于修复前,但其填充轮均为普通收敛轮(无中间文本),证据不受影响,记档。

## 2026-09-08 · Code Review 结论与修复波(独立 subagent 评审)

- **用户关键原话**:工作要求「全程走 Superpowers 流程」;ch06 先例「逐文件评审 git diff……结论分级 Critical/Important/Minor:Critical 和 Important 必须修复且每个修复带回归测试;Minor 可修复或记档(记档要写理由)。修复后全量 pytest 保持全绿」。
- **关键产出**:独立 subagent 全量评审 `git diff b7a533e..HEAD`(49 文件),复跑 293 passed 确认现状;结论 **0 Critical / 3 Important / 11 Minor**。修复(每条带回归测试,终态 **292 passed, 1 xfailed**——test_history 迁移净减 3 用例、回归净增 2):
  1. **[I-1] 窄化指令 `{order_id}` 占位符未格式化(ch06 回归)**:ch07 重写 agent 组装时丢了 `.format(order_id=…)`,字面量泄漏进背景块;旧测试断言被【订单数据】JSON 掩盖。修复 agent.py 先 format 再拼;回归断言「判断这一单(订单1001)能不能退」出现在背景块且无 `{order_id}` 字面量;线上复验:demo 服务器真实退款轮 model_ctx 中为格式化后文本。
  2. **[I-2] `updated_at` 常规轮不推进,侧栏「新在前」退化为创建序**:`store.append` 顺带 `UPDATE conversations SET updated_at=NOW()`;回归测试「旧会话续聊后上浮」(SQLite 秒级精度,测试内拉 2.1s 时差)。
  3. **[I-3] `render_history_text` 层 2 行丢角色前缀**:与层 1 同套「用户:/客服:」;回归断言三种行前缀。
  4. Minor 修复 6 条:M-1 `trim_history`/`history_to_messages` 彻底退役+test_history/test_chat_api 迁移(退役原定 Task 3,过渡期保绿推迟至此,spec 偏差回填);M-2 conftest 替换 store 后同步重建 SummaryService(不再持 MySQL factory);M-4 侧栏瞬时失败后可自愈恢复可见;M-5 场景 b 补「层1 降级」断言+用法注释补 s 场景;M-6 `SummaryService` 去掉 settings 死参数(全部调用点同步);M-7 `set_layer1_from` 补会话行 None 防御(与 get_anchors 对称)。
  5. Minor 记档 5 条:M-3 层 2 渲染块被丢时 history_text 仍按未丢弃行渲染(resolve 当轮窗口略大,无害,spec 已知边界);M-8 model_ctx tokens≈ 不计纯工具调用消息参数(日志口径);M-9 日志路径相对 CWD(约定仓库根启动);M-10 截断标记反超阈值(现实配置不可达);M-11 多 worker 边界(README 已知边界补句)。
- **拒绝或纠偏**:无(评审三条 Important 均属实,全部采纳;修复不改变任何验收证据,场景 B/S 的既有证据有效)。
- **翻车与返工**:I-2 回归测试首跑失败——SQLite CURRENT_TIMESTAMP 秒级精度,1.1s 时差撞同一秒,拉到 2.1s + 追加 1.1s 后通过;另 sed 插行位置插错一次,读文件后重写。
