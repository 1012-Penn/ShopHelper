# ch05 开发过程留痕(Workflow 编排 + LangGraph 图骨架)

## 2026-09-07 · brainstorm 定稿

- **用户关键原话**:需求全文见会话(Workflow 骨架 + 祛魅热身 + 七类意图四出口 + 置信度闸 + ReAct 主力 Agent + 转人工/建工单前端自选);澄清问答选了四个推荐项(裸循环进 scripts 演示、直接替换 /api/chat、MemorySaver、MySQL 保留双写);定稿批准原话:**「行,后续你都自己操作,我不过目了」**。
- **关键产出**:
  - 分类为 Architectural 路径;项目现状摸底(ch02 五工具 `app/tools/definitions.py`、ch04 `RetrievalService` low_confidence 语义、SSE 流式路由、单页前端);
  - langgraph 未装 → venv 安装 **langgraph 1.2.11 + langgraph-checkpoint 4.2.0**(langchain 1.4.0 原有);API 以 Context7 官方文档(streaming/memory 章节)+ 装好的 venv 源码签名双重核对(`StateGraph.add_node/add_conditional_edges`、`InMemorySaver`、`astream(stream_mode=[...], version="v2")`、节点内 `get_stream_writer()`/`StreamWriter` 注入);
  - spec 落盘并提交:`docs/superpowers/specs/2026-09-07-ch05-workflow-orchestration-design.md`(自审过:占位符/一致性/歧义清理,intent 兜底定为解析失败归商品咨询)。
- **两个关键选型**(spec 有备选与理由):①Agent 节点手写 ReAct 循环,不用 `create_react_agent` 预制件(祛魅主题 + 停止条件/预算可控);②流式走 `stream_mode=["custom"]` + `get_stream_writer`,弃 `astream_events`(噪音大)。
- **拒绝或纠偏**:无(用户全盘认可推荐项)。
- **翻车与返工**:Context7 首查连续 fetch failed、WebFetch 代理拒连(ECONNREFUSED 127.0.0.1:7897);改 pip 装包读源码签名 + Context7 稍后重试成功,未走凭记忆路线。

## 2026-09-07 · 计划评审通过 + 执行开始

- **用户关键原话**:「行,后续你都自己操作,我不过目了」(授权全自主,spec/计划评审门由我自审代行)。
- **关键产出**:`docs/superpowers/plans/2026-09-07-ch05-workflow-orchestration.md`(9 任务,每任务 TDD 五步);langgraph>=1.2.11,<2 进 pyproject。自审结论:spec 九节全覆盖、无占位符、节点签名统一 `(state, writer=None)`;补了两处计划初稿遗漏(ChatState 增 low_confidence/low_reason、make_agent_node 增 pool 参)。
- **拒绝或纠偏**:writing-plans 默认推荐 subagent-driven 执行;鉴于本计划与现有 fake 替身模式(ch04 Chunk/ScriptedToolModel)强耦合、共享上下文价值高,选 inline(executing-plans)执行——记为有意偏离,非用户否决。
- **翻车与返工**:无。
