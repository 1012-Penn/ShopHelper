# ch08 开发过程留痕(即插即用工具系统:注册中心 / 校验 / 权限 / 执行引擎 / 审计 / MCP / 建工单确认流)

## 2026-09-09 · brainstorm 定稿

- **用户关键原话**:需求全文见会话(注册中心/JSON Schema 校验/权限控制/执行引擎/审计留痕/MCP 双 Server/建工单确认流;「没有确认的调用引擎直接拒绝,不给模型绕过去的机会」「重试只给网络抖动这类暂时性故障……写操作默认不自动重试(超时未必没执行,重复执行比失败更糟)」「外部 MCP 工具的用途声明是对方自己写的、不可信,能不能调(尤其写操作)只认我们这侧的权限规则」「物流查询由物流 MCP Server 接管,第 2 章内置的 query_logistics 从清单里下线,别留重名工具」「里面什么superpowers相关的东西,你就全部选最推荐的」);DDL 随需求给出。用户就寝,全自主授权。
- **关键产出**:
  - 分类 Architectural;摸底:内置工具已是 langchain `@tool`(自带 name/desc/args_schema),注册/执行在 `ToolRegistry`(超时 10s、无条件重试 1 次);create_ticket 模型直调直执行;六个内置工具。
  - **依赖落位**:pip 装 mcp 1.30.0 / langchain-mcp-adapters 0.3.2 / jsonschema 4.26.0;内省确认 `MultiServerMCPClient(connections dict).get_tools()` 与 `FastMCP.streamable_http_app()` 接口面;Context7 核对 adapters 官方口径(`{"transport":"http","url":…}`)。
  - **spike×2 前置排雷**:①`interrupt()` 在 py3.10 + langgraph 1.2.11 async 上下文复现 `RuntimeError: Called get_config outside of a runnable context`(ch06 同款守卫)——需求字面的「用 LangGraph interrupt」不可行,确认流定稿**订单选择器同款无状态回传**(用户原话「照第 6 章订单选择器的做法」即该模式,功能语义完整保留);②MCP 全链 spike 通过:FastMCP(streamable_http_app)→uvicorn→MultiServerMCPClient 发现工具→ainvoke 拿回内容块。
  - spec 落盘:`docs/superpowers/specs/2026-09-09-ch08-tool-system-design.md`(自审:占位符/一致性/歧义/范围;审计语义、重试分类、重名策略、debug 工具开关均写死)。
- **自决决策点**(用户就寝,按「全部选最推荐的」+ ch06/ch07 推荐项自决先例):①确认流机制=无状态回传(spike 证据,功能等价);②MCP 发现频率=每轮聊天前 sync(TTL 30s 防抖)兑现「现问现拿」;③被拦截的 create_ticket 直调即落「权限拒绝」审计,确认执行单独落「成功」(每调用一条+验收 5 口径);④MCP 工具我侧全定只读;⑤debug 慢工具走 `TOOLS_DEBUG` 开关;⑥执行沿用 inline(executing-plans)而非 subagent-driven——替身底座强耦合、共享上下文价值高(ch06/ch07 同款偏离,用户「全部选最推荐」指流程选择器默认项,执行方式延续已验证惯例)。
- **拒绝或纠偏**:无(用户规格即定稿;自决项可纠偏)。
- **翻车与返工**:无(brainstorm 期;两颗雷 spike 前置排掉,计划期零返工)。
