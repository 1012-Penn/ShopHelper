# ch08 开发过程留痕(即插即用工具系统:注册中心 / 校验 / 权限 / 执行引擎 / 审计 / MCP / 建工单确认流)

## 2026-09-09 · brainstorm 定稿

- **用户关键原话**:需求全文见会话(注册中心/JSON Schema 校验/权限控制/执行引擎/审计留痕/MCP 双 Server/建工单确认流;「没有确认的调用引擎直接拒绝,不给模型绕过去的机会」「重试只给网络抖动这类暂时性故障……写操作默认不自动重试(超时未必没执行,重复执行比失败更糟)」「外部 MCP 工具的用途声明是对方自己写的、不可信,能不能调(尤其写操作)只认我们这侧的权限规则」「物流查询由物流 MCP Server 接管,第 2 章内置的 query_logistics 从清单里下线,别留重名工具」「里面什么superpowers相关的东西,你就全部选最推荐的」);DDL 随需求给出。用户就寝,全自主授权。
- **关键产出**:
  - 分类 Architectural;摸底:内置工具已是 langchain `@tool`(自带 name/desc/args_schema),注册/执行在 `ToolRegistry`(超时 10s、无条件重试 1 次);create_ticket 模型直调直执行;六个内置工具。
  - **依赖落位**:pip 装 mcp 1.30.0 / langchain-mcp-adapters 0.3.2 / jsonschema 4.26.0;内省确认 `MultiServerMCPClient(connections dict).get_tools()` 与 `FastMCP.streamable_http_app()` 接口面;Context7 核对 adapters 官方口径(`{"transport":"http","url":…}`)。
  - **spike×2 前置排雷**:①`interrupt()` 在 py3.10 + langgraph 1.2.11 async 上下文复现 `RuntimeError: Called get_config outside of a runnable context`(ch06 同款守卫)——需求字面的「用 LangGraph interrupt」不可行,确认流定稿**订单选择器同款无状态回传**(用户原话「照第 6 章订单选择器的做法」即该模式,功能语义完整保留);②MCP 全链 spike 通过:FastMCP(streamable_http_app)→uvicorn→MultiServerMCPClient 发现工具→ainvoke 拿回内容块。
  - spec 落盘:`docs/superpowers/specs/2026-09-09-ch08-tool-system-design.md`(自审:占位符/一致性/歧义/范围;审计语义、重试分类、重名策略、debug 工具开关均写死)。
- **自决决策点**(用户就寝,按「全部选最推荐的」+ ch06/ch07 推荐项自决先例):①确认流机制=无状态回传(spike 证据,功能等价);②MCP 发现频率=每轮聊天前 sync(TTL 30s 防抖)兑现「现问现拿」;③被拦截的 create_ticket 直调即落「权限拒绝」审计,确认执行单独落「成功」(每调用一条+验收 5 口径);④MCP 工具我侧全定只读;⑤debug 慢工具走 `TOOLS_DEBUG` 开关;⑥执行沿用 inline(executing-plans)而非 subagent-driven——替身底座强耦合、共享上下文价值高(ch06/ch07 同款偏离,用户「全部选最推荐」指流程选择器默认项,执行方式延续已验证惯例)。
- **拒绝或纠偏**:无(用户规格即定稿;自决项可纠偏)。
- **翻车与返工**:无(brainstorm 期;两颗雷 spike 前置排掉,计划期零返工)。

## 2026-09-09 · 计划评审通过 + 执行开始(inline)

- **用户关键原话**:「里面什么superpowers相关的东西,你就全部选最推荐的」「做完之后再提交GitHub一次」。
- **关键产出**:`docs/superpowers/plans/2026-09-09-ch08-tool-system.md`(9 任务:1 审计表+配置,2 注册中心+插件化,3 执行引擎,4 MCP 双 Server+客户端同步,5 接线改造,6 前端预览卡(Vibe),7 验收脚本+真机六条,8 code review,9 finish+push)。自审:spec 七个功能点均有任务对应(注册/校验/权限/引擎/审计/MCP/确认流),接口签名跨任务一致。
- **拒绝或纠偏**:writing-plans 推荐 subagent-driven,延续 ch06/ch07 记档理由选 inline——工具层与 GraphChatModel/conftest 替身底座强耦合,共享上下文优先;属有意偏离非用户否决。
- **翻车与返工**:无(计划期)。

## 2026-09-09 · Task 1-5 完成(审计/注册中心/引擎/MCP/接线,320 passed)

- **用户关键原话**:「所有工具调用走同一处,统一管超时、重试、错误处理、结果格式化」「写操作由执行引擎把门」「审计表不挂外键,写审计失败不许反过来拦工具执行」。
- **关键产出**:
  - Task 1:`tool_audit_logs` DDL(用户给定)入 `db/ch08-tool-audit.sql`+init.sql;ORM `ToolAuditLog`;`ToolAuditStore.log`(截 500);配置 `tool_timeout_seconds/tool_retry_attempts/tools_debug/mcp_*_url`。
  - Task 2:`ToolRecord`/`ToolRegistryV2`/`ToolContext`(base.py);`builtin.register_builtin`(四工具;query_logistics 内置下线);plugins 目录 autoload(`register(ctx)` 钩子,load_plugins 支持 directory 注入);示范插件 `demo_time.py`;debug 慢工具;definitions.py 转 shim 保旧消费者,registry.py 暂留。
  - Task 3:`ToolEngine.execute` 管线——jsonschema 校验(拦下回灌修正指引)/写权限把门(直调审计「权限拒绝」+on_write_intercept 回调推预览)/读暂时性故障重试(超时/连接类)、真故障与业务空结果不重试、写恒不重试/MCP 内容块取 text/审计全出路 try-except 包裹;`execute_confirmed` 给确认回传放行。13 项管线测试。
  - Task 4:`app/mcp_servers/logistics.py`(logistics_tracker)与 `aftersales.py`(query_warranty/query_return_progress + AFTERSALES_EXTRA_TOOLS 门控 query_repair_shop);`McpService.sync`(TTL 30s,diff 同步:新登记/消失下线/内置优先跳过/连接失败 warn 降级)。7 项 stub 测试。
  - Task 5:agent 切 `engine.execute`(带 conversation_id/tool_call_id/拦截回调发 ticket_preview 帧);chat.py 轮前 `mcp_service.sync()`;tickets 路由加 `POST /api/tickets/confirm`(confirmed 放行)与 `/cancel`(审计权限拒绝);ch05 按钮建单改 `confirmed=True`(点按钮即确认);main/conftest 新装配;旧 registry.py/definitions.py 删除,test_tools/test_registry/test_ch05/test_ch06 迁移(helpers 增 `builtin_engine()` 底座)。e2e 5 项(拦截/确认/取消/404/MCP 缺席降级)。
- **拒绝或纠偏**:无。翻车与返工(均为即逮即修):conftest/main import 面漏装(KnowledgeBaseStore/make_retrieval_chain);shim 构造逻辑一次错映射(embedder≠service)重写;naive_agent 祛魅脚本补审计库;一次 Edit 误删 renderRefundForm 声明行,同轮恢复。
