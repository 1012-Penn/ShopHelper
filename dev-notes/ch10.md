# ch10 开发过程留痕（微调多标签主题分类器）

## 2026-09-10 · brainstorm 定稿

- **用户关键原话**：「微调一个多标签主题分类器,把飞轮攒下的低置信度问题按主题批量归类,好决定先补哪块知识」；「全程走 Superpowers 流程,技能自动触发……产出不是可单测代码的任务(纯 Prompt、数据类、训练类),把 TDD 那步换成拿标注样例或评估集跑一遍验证,其余步骤照走;前端部分是例外,用 Vibe Coding 方式直接做」；「上面点名的技术选型是定死的,实现中发现矛盾或走不通,停下来问我」；「看看这次的工作要不要我跟进,如果不需要我守在旁边的话,你自己把它慢慢干完.提交到 GitHub」。
- **关键产出**：分类为 Architectural；spec 已写入 `docs/superpowers/specs/2026-09-10-ch10-topic-classifier-design.md`（17 类术语表进 `app/topic_taxonomy.py` 唯一权威、清洗/分层/增强为纯函数可单测、CPU 保守档全参微调、ONNX+onnxruntime 旁路批量服务、用户 DDL 原样采纳、分布页纯 CSS 条形图）。
- **拒绝或纠偏**：无（用户缺席，无法纠偏；六个代决策点已列 spec 末表，回来可否决）。
- **翻车与返工**：环境实测发现无 GPU（torch 1.12 CPU 系统级旧装）、主 venv 无训练依赖、池内真实问题仅 5 行（3 个缺口）——已在 spec「环境约束」记录，语料以照术语表造模拟问题为主（用户原预案），训练档位按 CPU 收敛到 max_len 96 / batch 16 / ≤8 轮早停。
- **流程说明**：brainstorming 技能的「逐问澄清 + 设计分节确认 + spec 用户审」三个停等门，因用户明示「不需要我守在旁边就自己干完」改为代决策推进并全程留痕；选择清单类问题一律先查实环境再定，未凭空拍板。
