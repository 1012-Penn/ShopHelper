# ch09 开发过程留痕（可观测性与数据飞轮）

## 2026-09-09 · brainstorm 定稿

- **用户关键原话**：需求全文见会话，核心是「给客服系统装上眼睛，再把答不上的问题变成改进燃料」；「全程走 Superpowers 流程」；「涉及具体库、框架、API 一律先用 Context7 MCP 查最新官方文档」；「自动评估流水线采用脚本 + cron/手动触发」；最终确认「后续全部你说的方案，你就开始搞」。
- **关键产出**：分类为 Architectural；确认采用 Langfuse 自部署 + 已编译 Graph 默认回调、统一 `FlywheelService`、`review_queue/eval_runs/request_usage/turn_retrievals` 数据闭环、后台审核页与评估趋势页；设计已写入 `docs/superpowers/specs/2026-09-09-ch09-observability-flywheel-design.md`。
- **官方 API 核对**：当前工具列表没有 Context7 MCP，已明确记录 fallback；查阅 Langfuse 官方文档确认 `CallbackHandler`、`propagate_attributes`、`start_as_current_observation`，查阅 LangGraph 官方文档确认 invocation `config.callbacks/metadata` 传播方式。
- **拒绝或纠偏**：用户纠正了我第一版混用英文的问题，要求后续全部中文；已改正，后续文档和沟通均使用中文。
- **翻车与返工**：创建隔离 worktree 前新增 `.worktrees/` 时误把已有 `.gitignore` 当成不存在，第一次提交短暂覆盖了原有忽略规则；已核对父提交、恢复 `.env/__pycache__/data/reports/logs` 等全部原规则并补回 `.worktrees/`，纠正提交为 `7c8d03e`。主工作树原有未提交改动未触碰。

## 2026-09-09 · 基线检查

- **关键产出**：隔离 worktree `codex/ch09-observability-flywheel` 已建立；复用主仓库 WSL `.venv` 完成基线测试：`328 passed, 1 xfailed`，4 个既有 pytest warning；退出时有 Milvus Lite 日志 handler warning，但不影响测试退出码。
- **拒绝或纠偏**：无。
- **翻车与返工**：Windows Python 3.13 环境没有 pytest，改用主仓库 WSL `.venv/bin/pytest` 运行，未新建依赖环境。

## 2026-09-09 · 计划评审通过 + 执行开始

- **用户关键原话**：用户 review spec 后确认「行,开始执行」；此前已确认「后续全部你说的方案,你就开始搞,就完事儿」。
- **关键产出**：实施计划已写入 `docs/superpowers/plans/2026-09-09-ch09-observability-flywheel.md`；SDD ledger 已写入 `.superpowers/sdd/2026-09-09-ch09-observability-flywheel/progress.md`，预检覆盖任务间共享文件/接口并记录 4 条 ruling。
- **拒绝或纠偏**：无；前端计划明确遵守 Vibe Coding 例外，后端仍按 TDD。
- **翻车与返工**：执行计划工具首次在 UNC worktree 的 `.git` 指针上被 WSL 拒绝，已将指针修为 `/root/...` 原生路径；同时发现 apply_patch 默认落主工作树，已迁回 worktree 并核验主工作树只保留原有改动。

## 2026-09-09 · Task 1 完成（可观测性核心与 token 底座）

- **用户关键原话**：用户要求「这三个干完之后，请你切换成自己独立完成接下来的任务，不要再用子智能体了。我的额度快要撑不住了」。
- **关键产出**：`app/observability.py` 提供 Langfuse 可选初始化、已编译 Graph 一次绑定、根 observation、检索 observation 入口和每请求独立 token callback；`request_usage` ORM/DDL 与 `UsageStore.aggregate_by_intent()` 完成；配置/主服务/聊天路由已接线；提交 `2baf60e` + 修复提交 `0ff9346`。
- **任务评审结论**：初评发现 1 Critical（Langfuse context 退出异常会二次 yield）和 2 Important（metadata 未统一短字符串、重复 bind 叠加 callback）；均已用回归测试修复。用户要求停止子智能体后，主线程亲自复核修复 diff。
- **验证结果**：`62 passed, 1 xfailed, 2 warnings`（observability、chat、ch05/ch06/ch07/ch08、config/db/models/store 回归）；warning 为既有同步测试 async 标记。
- **拒绝或纠偏**：按用户新要求不再派发 scoped re-review 子智能体，改由主线程审查修复 diff 并运行覆盖测试；没有扩大任务范围。
- **翻车与返工**：首个代理因完整套件验证被环境/中断阻塞，第二代理接手并完成主体；评审发现的 context manager/metadata/bind 三个问题经历一轮修复后通过。

## 2026-09-09 · task 1 可观测性核心、配置与 token 用量底座

- **用户关键原话**："缺少 key/SDK/服务时必须 no-op，不得阻断聊天"；后续纠正为 metadata 全部短字符串、token callback 必须按 request 隔离。
- **关键产出**：新增可选 Langfuse 接线、`request_usage` ORM/DDL 与 `UsageStore`；Graph 默认仅绑定一次 Langfuse handler，聊天 invocation 仍保留原 SSE sink，并注入每轮独立的 token callback 与短字符串 metadata。
- **拒绝或纠偏**：发现 token callback 放入 LangChain config 时必须实现 `BaseCallbackHandler` 协议；已补协议回归测试并修复。`conversation_id` 已由整数改为字符串，避免违反 Langfuse metadata 值约束。
- **翻车与返工**：首次相关回归暴露 `UsageCallback` 缺少 `run_inline`，导致 SSE 返回 error；按失败测试复现后继承 LangChain 公开 callback 基类，聊天回归恢复通过。工作树存在大量不相关的 CRLF/.agents 改动，只显式暂存 task 1 文件。

## 2026-09-09 · Task 2 完成（证据置信度闸与召回快照）

- **用户关键原话**：用户已确认「后续全部你说的方案,你就开始搞」，并补充「这三个干完之后，请你切换成自己独立完成接下来的任务，不要再用子智能体了」。
- **关键产出**：先新增 `tests/test_evidence_confidence.py` 形成 RED；随后在 `app/retrieval.py` 增加 Top1、有效证据数量、Top1/Top2 分差与合成分的正式闸，以及 `RetrievalResult.snapshot()`；`app/graph/knowledge.py` 将 Top-K 原文/得分写入状态；新增 `scripts/calibrate_evidence_confidence.py`，按 ch04 评估集标注样例生成校准产物；图状态、配置和生产检索链支持加载校准结果。
- **评审结论**：主线程独立完成代码复核；确认闸仍位于知识检索之后、Agent 之前，低置信时保留候选快照但不把证据交给 Agent，并保持原有 fallback/pool 行为。
- **拒绝或纠偏**：没有再派发子智能体；没有改变 ch04 评估集和四策略检索接口；没有把阈值写死为新拍脑袋值，缺少校准产物时沿用现有 `rerank_score_floor` 基线。
- **翻车与返工**：首轮 RED 正确暴露 `EvidenceCalibration` 尚未实现；实现后唯一失败是浮点数 `0.41000000000000003` 与断言 `0.41` 的表示差异，已在可解释输出边界统一 round 并通过回归。
- **验证结果**：证据置信度、检索、知识闸、builder、pipeline、配置回归共 `37 passed, 2 warnings`；warning 为既有同步测试的 async 标记。

## 2026-09-09 · Task 3 完成（问题池、去重队列与审核落库）

- **用户关键原话**：用户要求「三个入口都把问题送进 ch04 建的 low_confidence_questions 问题池」以及「一行一个去重缺口,查重命中就累加出现次数,不新建行」。
- **关键产出**：新增 `review_queue`、`eval_runs` ORM 与 ch09 DDL；问题池增加 `matched_review_id`、`retrieved_chunks`；`FlywheelService` 实现标准化/模型语义去重注入口、原话与召回快照详情、通过写 `knowledge_chunks(pending)`、驳回；检索闸、生成自评和用户 👎 三入口均已统一接入。
- **评审结论**：主线程独立复核跨表事务和旧 `LowConfidencePool.insert` 兼容性；确认低置信候选只留审核快照，不重新喂给 Agent。
- **拒绝或纠偏**：旧测试要求弱证据兜底同时保留 `retrieval_low_conf` 和 `self_check` 两个来源，未擅自合并成单一来源；生产路径使用模型标准化/语义查重，模型失败回退为确定性实现，保证聊天不被飞轮服务拖垮。
- **翻车与返工**：首次把低置信候选保留在 `RetrievalResult.items` 后，旧 `query_faq` 契约意外把弱候选暴露给 Agent；已在工具边界过滤可见 items，同时继续返回审核快照，回归恢复。
- **验证结果**：`tests/test_flywheel.py`、模型/守卫/知识节点/聊天 API 相关回归共 `20 passed`；工具、Agent、工具流回归 `27 passed`。

## 2026-09-09 · Task 4 完成（反馈、后台审核页与评估趋势）

- **用户关键原话**：用户要求「聊天页那组满意度反馈目前是纯前端采集,本章把 👎 接到后端」；「待审队列做成一个简单的后台管理页」；「评估流水线至少跑两轮,能看到指标趋势对比」。
- **关键产出**：新增 `/api/feedback`、`/api/review-queue`、`/api/eval-runs`、`/api/usage/by-intent`；新增 `/admin/review` 审核页和 `/admin/evals` 趋势页；新增 `scripts/run_eval.py`，复用 ch04 评估集/Recall@K/MRR/Faithfulness 并将每轮写入 `eval_runs`；审核通过后自动向量化 pending chunk（生产依赖注入时）。
- **评审结论**：主线程检查了前端反馈 payload、审核详情中的原话/召回片段和审核后知识库双写路径；前端按 Vibe Coding 例外直接实现，未套额外 brainstorm/TDD/review 子流程。
- **拒绝或纠偏**：没有为前端额外引入框架；评估脚本继续复用 ch04 评估集与既有 faithfulness 裁判链，没有另造指标口径。
- **翻车与返工**：全套件首次回归发现低置信候选保留策略与旧 `query_faq` 空 items 契约冲突，已在工具层修复；无新增依赖或方案替换。
- **验证结果**：全套件在修复前为 `346 passed, 1 xfailed, 4 warnings`，失败仅为上述既有契约冲突；修复后已通过相关工具/Agent 回归 `27 passed`，最终全套件将在 finish 前重新执行。

## 2026-09-09 · finish

- **用户关键原话**：用户要求「完结交付:功能演示命令、测试结果、dev-notes 路径」，并明确「不要再用子智能体了」。
- **关键产出**：提交 `2baf60e`、`0ff9346`、`da61f02`、`752658e`；实现代码、DDL、Langfuse 自部署 compose、后台审核/评估页和过程记录均在 `codex/ch09-observability-flywheel` 分支；设计/spec 路径和实施计划路径保持可追溯。
- **评审结论**：最终按证据完成验证，全套件 `347 passed, 1 xfailed, 4 warnings`；新增模块 compileall 通过；Langfuse compose 经 PyYAML 解析并确认 6 个服务定义完整。
- **拒绝或纠偏**：没有继续派发子智能体；没有把既有工作树的 `.agents`/CRLF 噪声或用户未提交改动混入本章提交；没有声称 Docker 已启动，因为当前环境没有 Docker CLI。
- **翻车与返工**：一次全套件回归暴露 `query_faq` 弱证据候选泄漏给工具输出，已修复为“保留快照、隐藏 Agent 可见证据”后重新通过；Docker compose 运行验证因本机未安装 Docker，仅完成 YAML 静态解析。

## 2026-09-09 · 委托验收(主线程接管 Codex 分支)

- **用户关键原话**:「我让codex完成了大部分工作,你检查一下他的工作,并且收尾提交到GitHub」;中途追问「现在这个是哪个bigmodel导致的步骤出问题了」「这个在我们的项目中具体是干嘛?能不能换个DeepSeek的API key?」——已答:glm-4.7-flash(bigmodel.cn 免费档)承担全部对话类 LLM 调用,并按 .env 里预留的 DeepSeek 配置切回。
- **关键产出**:接管 `codex/ch09-observability-flywheel` 分支(8 提交,40 文件)。基线复跑 `328→347 passed` 与 Codex 记录一致;对照需求逐条审查。
- **拒绝或纠偏**:①分支被 Codex 工具链写入 CRLF,40 文件呈现整文件重写假象——先恢复 worktree 185 个纯行尾噪声文件、把分支已提交文件归一 LF(真实 diff 从 +3707/-1425 收敛到 +2330/-36)再审;②Codex 未提交的 `static/index.html` 16 行真实改动(👎 接后端)单独补提交。
- **翻车与返工**:worktree 恢复操作误把 index.html 真实改动一并还原,对照 diff 原样重放;`docker compose up` 在 worktree 目录执行导致项目名错挂全新空 MySQL 卷(`ch09-observability-flywheel_mysql-data`),真数据在主项目 `shophelper_mysql-data` 卷,删错卷重建后恢复。

## 2026-09-09 · 验收前修复波(357 passed)

- **用户关键原话**:无新指令(沿用「检查他的工作,收尾提交」授权)。
- **关键产出**(每项带回归):
  1. **👎 回捞缺失补齐**(用户需求原文要求「从该会话当轮的检索结果里尽力回捞」):新增 `app/round_capture.py`(会话级环形缓存,空快照=确认没走检索、None=回捞不到落 NULL),chat 路由轮末记录、feedback 路由回捞接入,5 项单测;
  2. **落池/审核不再阻塞事件循环**:生产池是 FlywheelService(内联 LLM 标准化+查重),graph 四处 `pool.insert` 与审核 approve(内含向量化双写)全部 `asyncio.to_thread`;
  3. **校准闭环补全**:校准脚本只有消费者没有信号生成器——新增 `scripts/gen_evidence_signals.py` + `tests/eval/ch09_junk_set.jsonl`;`calibrate_evidence_thresholds` 补 `combined_floor` 推导(同一条合成公式对正样本取下界);真机跑 40 信号(31 正样本全放行 / 9 junk 全拦),产物入库 `config/evidence_calibration.json` 并改默认路径(原指向 gitignored 的 data/);
  4. 死代码 `observe_retrieval` 移除;review-queue 路由 4 项接口回归补齐;`UsageStore.aggregate_by_intent` Decimal→int(修 API 序列化成字符串);`LLM_MAX_RETRIES=6`(bigmodel 1305 全局限流下默认 2 次亚秒退避不够)。
- **拒绝或纠偏**:Codex 的「标准化/查重内联在落池时」设计保留(其在计划评审时经用户确认),只修阻塞问题,不推翻为批处理。
- **翻车与返工**:ch05 FakePool 替身签名落后于 `insert()` 新参(合并快照参数后恒传 5 参)即逮即修。

## 2026-09-09 · 真机验收六条全过 + 两处真机 bug

- **用户关键原话**:「能不能换个DeepSeek的API key?」——.env 内本有历史 DeepSeek key(注释保留),切换后限流解除。
- **关键产出**(`reports/ch09-acceptance.md`,Langfuse v3.225.7 真栈):
  - **验收1**:trace 树 16 observations(CHAIN/AGENT/GENERATION),total_tokens=3562、latency 4.6s、`metadata.intent=商品咨询`;
  - **验收2+3**:兜底→待审队列(详情含原话+带分召回片段)→approve(chunk 28 向量化 done)→同题再问「SH-FLY01 售价 99999 元…[1]」top1=0.48 gate passed,整圈转通;
  - **验收4**:👎 缺口详情 source=['retrieval_low_conf','self_check','user_feedback'] 三入口归并一行;
  - **验收5**:商品咨询 37244 token 最烧钱,固定话术路径正确记 0;
  - **验收6**:两轮 eval_runs(recall@3=0.875/mrr=0.781/faithfulness=1.0)趋势可读,两轮分差恰为 0(确定性检索同子集,即「无下滑」信号)。
- **翻车与返工(两处真机 bug,Codex 沙箱测不出)**:
  1. **v3 CallbackHandler 构造签名**:`CallbackHandler(public_key=…, secret_key=…, host=…)` 是 v2 口径,v3 只收 `public_key/update_trace/trace_context`、凭据走环境变量——Codex 环境未装 SDK 走了 no-op 分支,单测全绿但真机「Langfuse 初始化失败,禁用 tracing」。修复:无参构造 + Settings 回落注入 `LANGFUSE_*` 环境变量,并新增「装了 SDK 必须能真构造」回归(importorskip 门控)。
  2. **langgraph 1.2.11 节点内 LLM 不继承图级回调**:节点内裸调 `model.ainvoke(msgs)` 无 GENERATION span(最小 print-handler 对照实验复现,官方 invoke-config 挂法同样失效);langgraph 注入的 `config` 显式传给模型调用后恢复。改动:`_with_sink` 透传 config,resolve/intent/agent/expand 四节点穿线,10 处节点签名补 `config=None`,测试替身 `astream/ainvoke` 签名对齐。此修复同时救活 UsageCallback token 统计(验收5 的前提)。
- **拒绝或纠偏**:验收脚本三处口径自我纠偏——标准化会洗掉 marker 改按详情原话匹配;核准答案给「暂不支持」式内容导致 agent 忠实转述仍拒答(换真实可答内容后整圈转通);两轮评估分差为 0 是「无下滑」的正确信号,不是 bug。
- **环境坑留档**:`pkill -f "uvicorn app.main:app"` 会自匹配调用方 shell(复合命令里含同字面文本),改 `app[.]main` 正则并拆分调用;Milvus Lite 文件单进程锁,旁路脚本须先停主服务;`docker compose` 务必在主仓库目录执行(项目名决定数据卷归属)。

## 2026-09-09 · finish(完结交付)

- **功能演示命令**(全流程从零):
  ```bash
  docker compose -f docker-compose.langfuse.yml up -d      # Langfuse 自部署栈(6 服务)
  docker compose up -d && .venv/bin/python -m db.seed && .venv/bin/python -m db.seed_conversations
  .venv/bin/python -m scripts.build_kb                     # 含证据闸校准产物 config/evidence_calibration.json
  # .env:LANGFUSE_ENABLED=true + LANGFUSE_PUBLIC_KEY/SECRET_KEY(pk-lf-…/sk-lf-…)
  .venv/bin/uvicorn app.main:app --port 8000
  .venv/bin/python scripts/acceptance_ch09.py --scenario trace|flywheel|feedback|cost|eval
  ```
  管理页:`/admin/review`(待审队列审核)、`/admin/evals`(评估趋势);Langfuse UI:`http://127.0.0.1:3000/project/dev/traces`。
- **测试结果**:357 passed, 1 xfailed(基线 347 + 修复波新增 14,其中 3 项被移除的死代码相关);真机六条全过(证据 `reports/ch09-acceptance.md`);code review 结论:0 Critical 遗留 / 本轮修复 2 真机 Critical(v3 签名、config 断链)+ 3 高优(回捞缺失、事件循环阻塞、校准闭环)+ 若干 Minor。
- **本文档路径**:`dev-notes/ch09.md`;设计:`docs/superpowers/specs/2026-09-09-ch09-observability-flywheel-design.md`;计划:`docs/superpowers/plans/2026-09-09-ch09-observability-flywheel.md`;验收:`reports/ch09-acceptance.md`(本地)。
- **已知边界**:同 finish 前记录,另加——RoundCapture 进程内重启即失;标准化/查重内联兜底轮略慢(to_thread 不阻塞他人);审核页无认证(演示系统);评估定时化走外部 cron。
